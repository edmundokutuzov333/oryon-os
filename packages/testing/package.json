{
  "name": "@oryon/testing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "workspace:*",
    "vitest": "^3.2.0",
    "@biomejs/biome": "workspace:*"
  }
}
